<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Основная тема приложения -->
    <style name="Theme.MRSU" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Основные цвета -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Цвета для элементов интерфейса -->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/gray</item>

        <!-- Стиль для ToolBar -->
        <item name="toolbarStyle">@style/ToolbarStyle</item>

        <!-- Стиль для элементов навигационного меню -->
        <item name="navigationViewStyle">@style/NavigationViewStyle</item>

        <!-- Настройки для элементов UI -->
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button.Colored</item>
        <item name="android:editTextStyle">@style/Widget.AppCompat.EditText</item>
        <item name="android:alertDialogStyle">@style/ThemeOverlay.AppCompat.Dialog.Alert</item>
    </style>

    <!-- Тема для темного режима -->
    <style name="Theme.MRSU.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Цвета для элементов интерфейса -->
        <item name="android:colorBackground">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/gray</item>
    </style>

    <!-- Стиль для ToolBar -->
    <style name="ToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/purple_500</item>
        <item name="android:titleTextColor">@color/white</item>
        <item name="android:subtitleTextColor">@color/white</item>
    </style>

    <!-- Стиль для NavigationView -->
    <style name="NavigationViewStyle" parent="Widget.Design.NavigationView">
        <item name="android:background">@color/white</item>
    </style>
</resources>
